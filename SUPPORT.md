# Getting support

Use repository issues for reproducible client bugs, export failures, and protocol integration questions. Include the operating system, Godot version, export target, server version, content schema/content ID, and redacted logs.

Do not post ROMs, `.monpack` files, credentials, tokens, keys, saves, or raw captures. For security-sensitive reports, follow `SECURITY.md` instead.
